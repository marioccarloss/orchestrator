# Post-change benchmark status — 2026-09-14

**Status: blocked; no valid post-change `BenchReport` has been produced.**

This file is a limitation record, not a benchmark result. An `after.json` was deliberately not synthesized because the available inputs cannot support the comparison required by T35.

## Evidence available

- `bench/corpus.json` validates 15 anonymized journeys across the three priority ecosystems.
- `bench/baselines/2026-09-baseline.json` contains `journeys: 0`; it is a schema fixture/placeholder, not an observed pre-change baseline.
- The current workspace registry does not contain the Inditex or Prensa repositories needed for J01–J10.
- The only retained OpenReferences Flow capture predates this benchmark run, does not correspond to a corpus journey, contains no token-bearing messages, and is still marked active. Re-labeling it would be misleading.
- Static verification is green (`bun run check`: 252 tests across 39 files), but tests do not prove model-driven journey execution or token savings.

## Metrics that remain unmeasured

| Metric | Target | Status |
|---|---:|---|
| Lite one-shot rate | ≥ 85% | Not measured |
| Full one-shot rate | ≥ 70% | Not measured |
| Insufficient-evidence precision | ≥ 90% | Not measured |
| Stale-context incidents | 0 | Not measured on the corpus |
| Tokens per accepted delivery | −40% vs. baseline | Cannot compare against a zero-journey baseline |
| Rediscovery ratio | ≤ 10% | Not measured |
| Coverage honesty | 100% | Covered by deterministic tests only |
| Language compliance | 100% | Covered by deterministic tests only |

## Required to close T35

1. Provide isolated fixtures or checkouts for every `repoProfile` in the corpus, including Inditex and Prensa.
2. Execute all journeys with the pre-change implementation and preserve real Flow metrics/events as the observational baseline.
3. Execute the same corpus with the post-change implementation under equivalent model/provider conditions.
4. Classify one-shot acceptance, necessary evidence stops, unnecessary reads, language compliance, and failure causes from the captured runs.
5. Aggregate the real outcomes into `bench/baselines/2026-09-after.json` and record explanations for every missed target.
